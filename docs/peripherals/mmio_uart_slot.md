# MMIO UART Peripheral

UART peripheral connected to the MMIO subsystem.

---

## Function

The UART peripheral provides serial communication between
the MicroBlaze CPU and external devices.

The peripheral supports:

- UART transmit
- UART receive
- Baud rate configuration
- RX FIFO status monitoring
- TX FIFO status monitoring

---

## MMIO Slot

- slot_1_uart

---

## RTL Modules

- `uart_core.vhd`
- `uart_tx.vhd`
- `uart_rx.vhd`
- `baud_gen.vhd`
- `fifo.vhd`

---

## Software Driver

- `chu_uart.c`
- `chu_uart.h`

---

## Base Address

```text
UART_BASE_ADDR = 0xC0000080
```

---

## Register Map

```text
offset 0 : RX data and status register (read)

         31                10 9          8 7              0
         +------------------+-------------+----------------+
         |     reserved     | TX_FULL     | RX_DATA[7:0]  |
         +------------------+-------------+----------------+


offset 1 : Baud divisor register (write)

         31                             11 10             0
         +--------------------------------+----------------+
         |            reserved            | DVSR[10:0]     |
         +--------------------------------+----------------+


offset 2 : TX data register (write)

         31                             8 7               0
         +--------------------------------+----------------+
         |            reserved            | TX_DATA[7:0]  |
         +--------------------------------+----------------+


offset 3 : RX removal register (write)

         31                                              0
         +------------------------------------------------+
         |               dummy write pulse                |
         +------------------------------------------------+
```

---

## Register Summary

| Offset | Register | Bits | Description |
|--------|-----------|------|-------------|
| 0x00 | RX_STATUS | [7:0] | received UART byte |
| 0x00 | RX_STATUS | [8] | RX FIFO empty flag |
| 0x00 | RX_STATUS | [9] | TX FIFO full flag |
| 0x00 | RX_STATUS | [31:10] | reserved |
| 0x04 | BAUD_DIVISOR | [10:0] | baud rate divisor value |
| 0x04 | BAUD_DIVISOR | [31:11] | reserved |
| 0x08 | TX_DATA | [7:0] | transmitted UART byte |
| 0x08 | TX_DATA | [31:8] | reserved |
| 0x0C | RX_POP | [31:0] | dummy write used to remove RX FIFO data |

---

## Notes

The UART peripheral uses separate read and write registers
for clarity and simplified MMIO access.

The RX removal register generates a pulse used to remove
one byte from the receive FIFO buffer.

---

## Baud Rate Configuration

The UART baud rate is generated by the internal baud generator.

The baud generator divides the FPGA system clock and produces a tick signal used by both the UART transmitter and receiver state machines.

The divisor register (`DVSR`) is programmed through the MMIO interface at offset 1.

The generated tick frequency is:

```text
f_tick = f_clk / (DVSR + 1)
```

The UART receiver and transmitter use a 16× oversampling clock, therefore the effective baud rate is:

```text
baud_rate = f_clk / ((DVSR + 1) × 16)
```

where:

```text
f_clk = FPGA system clock frequency
DVSR  = baud divisor register value
```

For a 100 MHz system clock and a target baud rate of 115200 baud:

```text
DVSR = f_clk / (baud_rate × 16) - 1

DVSR = 100000000 / (115200 × 16) - 1
DVSR ≈ 53
```

Therefore:

```text
DVSR = 53
```

generates approximately:

```text
baud_rate
```

